export class Recipe {
  // name: string;
  // instructions: string;
  // description: string;
  // ingredients: string[];
  // tags: string[];
  constructor(public name: string, public instructions: string, public description: string, public ingredients: string[], public tags: string[]) {}
}
